# Optional Vercel/serverless stats endpoint guard.
# Generate a long random value before enabling /api/stats.
STATS_ADMIN_KEY=

# Current open-source build uses BYOK in the browser for AI features.
# Do not put DeepSeek/OpenAI provider keys here unless you implement a server-side AI proxy.
